/** @type {import('next').NextConfig} */
const nextConfig = {
  disableManifest: true,

      images: {
        
        domains: [
              'lh3.googleusercontent.com'
        ]
      } 
}

module.exports = nextConfig
